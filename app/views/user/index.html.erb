自分が投稿したページ

<div class="container">
<div class="row">
<div class="col">

<hi>User info</hi>

  <h4>name</h4>

  <h4>introduction</h4>



<h1>New Book</h1>
<%= form_for(@book, url: '/books') do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %><!--キストを入力するためのフォーム-->

  <h4>本文</h4>
  <%= f.text_area :body %><!--長文用のフォーム-->

  <%= f.submit '投稿' %> <!--ここまできたら、自動的にcreateアクションへ飛ぶようになっている-->

<% end %>

<% if @book.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
</div>
</div>
</div>





<h1>Books</h1>
 <table border="0">

 <tr>
    <th>タイトル</th><th>Body</th>
</tr>
<tr>
	<% @books.each do |book| %>

    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <td><%=link_to "edit",  edit_book_path(book.id)%></td>
    <td><%=link_to "show",  book_path(book.id)%></td>
    <td><%= link_to "destroy",book_path(book.id), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
</tr>
      <% end %>
</table>



